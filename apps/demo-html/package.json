{
  "name": "@fs-ds/demo-html",
  "version": "0.0.1",
  "private": true,
  "description": "FS Design System — Plain HTML + CSS consumer demo",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@fs-ds/tokens": "*",
    "@fs-ds/web-components": "*"
  },
  "devDependencies": {
    "vite": "^5.2.0"
  }
}
